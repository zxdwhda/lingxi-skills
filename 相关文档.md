# 灵汐 API 端点速查表

> 本速查表基于实测验证整理，与灵汐官方文档互补。  
> **文档整理日期：2026-09-11。该日期是本速查表整理/归档日期，不代表表内所有端点都在当天重新全量复测。**  
> 模型、分组、渠道和上游可用性可能随时变化；实际使用前请以灵汐控制台、`GET /v1/models` 和官方文档为准。  
> 完整文档见：https://aicso.apifox.cn

---

## 一、OpenAI 兼容端点

Base URL: `https://api.aicso.top/v1`

| 功能 | 端点 | 已验证模型 | 备注 |
|------|------|-----------|------|
| 聊天 | `POST /chat/completions` | `gpt-4o`, `gpt-4o-mini`, `gpt-5`, `deepseek-chat`, `claude-sonnet-4-20250514` | 支持 stream、function calling、vision |
| 图片生成 | `POST /images/generations` | `dall-e-3`, `gpt-image-1` | 返回 `b64_json`，需解码保存 |
| 语音合成 | `POST /audio/speech` | `tts-1`, `gpt-4o-mini-tts` | 支持多种 voice |
| 语音识别 | `POST /audio/transcriptions` | `whisper-1` | 支持 timestamps |
| 文本嵌入 | `POST /embeddings` | `text-embedding-3-small` | |
| 获取模型列表 | `GET /models` | — | 返回当前分组可用的全部模型 |

### Claude Native 格式
- Base URL: `https://api.aicso.top`
- 端点: `POST /v1/messages`
- Header: `x-api-key`, `anthropic-version: 2023-06-01`
- 注意: `max_tokens` 必填，模型名需带完整日期后缀

### Gemini Native 格式
- Base URL: `https://api.aicso.top`
- 端点: `POST /v1beta/models/{model}:generateContent`
- 注意: 使用 `contents` + `parts` 结构

---

## 二、Native 原生端点

Base URL: `https://api.aicso.top`（**不带 `/v1`**）

### 图片
| 平台 | 端点 | 状态 |
|------|------|------|
| Midjourney | `POST /mj/submit/imagine` | ✅ 端点存在（上游负载影响可用性） |
| Midjourney 查询 | `GET /mj/task/{id}/fetch` | ✅ |
| Replicate | `POST /replicate/v1/models/{owner}/{model}/predictions` | ✅ |
| Replicate 查询 | `GET /replicate/v1/predictions/{id}` | ✅ |

### 视频
| 平台 | 端点 | 关键参数 | 备注 |
|------|------|---------|------|
| Kling 文生视频 | `POST /kling/v1/videos/text2video` | `model_name`, `mode`(std/pro), `duration`(字符串"5"/"10"), `aspect_ratio` | 异步 |
| Kling 图生视频 | `POST /kling/v1/videos/image2video` | `model_name`, `image`, `mode`, `duration` | 异步 |
| Kling 查询 | `GET /kling/v1/videos/text2video/{id}` | — | 返回 `task_status` |
| Sora/Veo 统一 | `POST /v1/video/create` | `model`, `prompt`, `size`, `duration`(4/8/12) | 异步 |
| Sora/Veo 查询 | `GET /v1/video/query?id={id}` | — | |
| Luma | `POST /luma/generations` | `user_prompt`, `model_name`, `duration`, `resolution` | 异步 |
| Luma 查询 | `GET /luma/generations/{id}` | — | 返回 `state` |
| MiniMax 海螺 | `POST /minimax/v1/video_generation` | `model`, `prompt`, `duration` | 异步 |
| MiniMax 查询 | `GET /minimax/v1/query/video_generation?task_id={id}` | — | |
| 豆包 | `POST /volc/v1/contents/generations/tasks` | `model`, `content` | 异步 |
| 豆包 查询 | `GET /volc/v1/contents/generations/tasks/{id}` | — | |

### 音频
| 平台 | 端点 | 备注 |
|------|------|------|
| Suno 提交 | `POST /suno/submit/music` | 异步 |
| Suno 查询 | `GET /suno/fetch/{id}` | 返回 `status`(SUCCESS/FAILURE) |
| Suno 歌词 | `POST /suno/submit/lyrics` | |
| MiniMax TTS | `POST /minimax/v1/t2a_async_v2` | 异步 |
| MiniMax TTS 查询 | `GET /minimax/v1/query/t2a_async_query_v2?task_id={id}` | |
| MiniMax 文件上传 | `POST /minimax/v1/files` | `purpose: voice_clone` |
| MiniMax 语音克隆 | `POST /minimax/v1/voice_clone` | |

### 系统管理（需管理员 Key）
| 功能 | 端点 |
|------|------|
| 获取令牌列表 | `GET /api/token/?p=0&size=10` |
| 创建令牌 | `POST /api/token/` |
| 删除令牌 | `DELETE /api/token/{id}/` |
| 修改令牌 | `PUT /api/token/` |
| 批量修改 | `PUT /api/token/batch` |
| 搜索令牌 | `GET /api/token/search?keyword=...` |
| 获取用量 | `GET /api/usage/token/` |
| 获取账号信息 | `GET /api/user/self` |
| 获取模型列表 | `GET /v1/models` |

---

## 三、图床

| 功能 | 端点 | 备注 |
|------|------|------|
| 上传图片 | `POST https://imageproxy.zhongzhuan.chat/api/upload` | **无需 API Key** |
| 支持格式 | JPG, PNG, GIF, WebP | |
| 建议大小 | ≤ 10MB | |
| 返回 | `{"url": "https://imageproxy.zhongzhuan.chat/api/proxy/image/..."}` | URL 长期有效 |

---

## 四、常见错误速查

| 错误信息 | 原因 | 解决 |
|---------|------|------|
| `无可用渠道` | 当前令牌分组不支持该模型 | 换分组或换模型 |
| `Invalid URL` | 非 OpenAI 端点错误加了 `/v1` | Native 端点用 `https://api.aicso.top` 不带 `/v1` |
| `上游负载已饱和` | 上游提供商暂时过载 | 稍后重试 |
| `all_retries_failed` | 上游渠道暂时不可用 | 稍后重试或换分组 |
| `401` | API Key 无效 | 检查 Key |
| `无权进行此操作` | 非管理员 Token 调用了系统接口 | 使用管理员 Key |
| `size is required for sora-2` | Sora 缺少必填参数 | 添加 `size` 和 `duration`(4/8/12) |
| `this channel is designed for task-based operations` | 使用了错误的端点格式 | 该模型需用 Native 端点而非 OpenAI 兼容端点 |
